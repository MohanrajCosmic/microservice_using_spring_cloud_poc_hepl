server:
  port: 8888
spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MohanrajCosmic/microservices_using_spring_cloud_poc_config_file_repos
          clone-on-start: true